<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_monochrome"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NARUCIVANJE_NOVO"
        tools:targetApi="31">

        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".QrScannerActivity" />
        <activity android:name=".OdabirJelaPicaActivity" />
        <activity android:name=".ModifikacijeActivity" />
        <activity android:name=".PregledNarudzbiActivity" />
        <activity android:name=".PregledMojihNarudzbiActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".PracenjeNarudzbeActivity" />
        <activity android:name=".AktivneNarudzbeActivity" />
        <activity android:name=".PotvrdaNarudzbeActivity" />
        <activity android:name=".AzuriranjeNarudzbeActivity" />
        <activity android:name=".KuhinjaActivity" />
    </application>


</manifest>